# Copy to .env and adjust. Every value here is the documented default, so an
# empty .env behaves identically — but a variable that is present must be
# valid: blank and malformed values are rejected rather than defaulted.

# development | test | production
NODE_ENV=development

# Server. One process serves the API and the console, so this is the only
# port there is.
HOST=0.0.0.0
PORT=3000

# debug | info | warn | error  (defaults to info in production, debug otherwise)
LOG_LEVEL=debug

# A pre-shared API key, so a deployment has a reproducible credential instead
# of one that only exists after somebody clicked through the setup screen.
# Optional: leave it unset and the console mints one on first run. When set it
# wins, is granted every scope, and must be at least 32 characters.
# API_KEY=

# Every rendered date and log timestamp uses this zone. Any IANA name the
# runtime knows; the server refuses to start on one it does not.
SERVER_TIMEZONE=Asia/Jakarta

# Logs go to the console and to a file under LOG_DIR. The file rotates by
# name — hourly | daily | weekly | never — so no restart is needed to roll it.
LOG_ROTATION=daily
LOG_DIR=./data/logs

# SQLite file. A bare path, a file: URL, or :memory:. Created on first run.
DATABASE_PATH=./data/db/bunwa.sqlite

# Refuse to start when migrations are pending, rather than applying them.
# Defaults to true in production, false otherwise.
MIGRATE_STRICT=false

# Writable directory for runtime state — chiefly the migrations materialised
# out of the binary. Ephemeral; nothing here is a source of truth.
RUNTIME_DIR=.runtime

# Mount the admin API. It can mint API keys and has no authentication yet, so
# enabling it is a deliberate act. Never true in a deployment until §1.3 grows
# operator sessions.
ADMIN_API_ENABLED=false

# Permit webhook targets that are plain http, or private/loopback addresses.
# Its own switch rather than derived from NODE_ENV: a security control must not
# be disabled by an unset environment name. Rejected outright in production.
ALLOW_INSECURE_WEBHOOK_TARGETS=false

# There is no second port. The console is served by the same app on PORT above,
# so it has no dev server of its own and no API target to configure — this
# block described DASHBOARD_PORT and BUNWA_API, neither of which anything reads
# since the console moved into this project.

# Devices per engine pool. Bounds the blast radius of one pool failing.
ENGINE_POOL_CAPACITY=25

# Encrypts WhatsApp credentials at rest.
#
# Run WhatsApp in this process. Off by default: the engine has never paired a
# real device, so a deployment opts in rather than being upgraded into it.
# There is no other engine now, so pairing needs this on.
BAILEYS_ENABLED=false

# Required when BAILEYS_ENABLED is on, because that engine keeps the noise key,
# signed identity key and adv secret — everything needed to take over the
# account — in this database. A gowa-only deployment does not need it: gowa
# holds those credentials, not bunwa.
#
#   openssl rand -hex 32
#
# Losing this key means every device re-pairs, so it belongs wherever the rest
# of the deployment's secrets live, not beside the database it protects.
CREDENTIAL_ENCRYPTION_KEY=
